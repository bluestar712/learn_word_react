export interface IWord {
    id: number,
    rus: string,
    tat: string,
}

export interface IPost {
    id: number;
    title: string;
    body: string;
}

export interface IUser {
    id: number;
    name: string;
    email: string;
}